# DCS-OPCT v11 manuscript revision log

## 2026-09-16: public repository deposition and v23 finalization

- Published the validated v22 reproducibility package, browseable source code,
  aggregate evidence, and validation reports at
  `https://github.com/dohonwei/dsc-opct`.
- Verified public repository metadata, the remote main-branch commit, the
  SHA-256 sidecar, and the exact uploaded local archive hash; recorded the
  evidence and the interrupted second-download attempt without overstating it.
- Replaced the anonymous-deposition placeholder in Data and code availability
  with the public author-identifiable repository URL.
- Closed reviewer task T8, removed the final author-dependent submission
  blocker, and prepared v23 packaging and validation without changing the
  frozen method, experimental outputs, or claim boundary.

## 2026-09-16: v22 submission-chain consistency audit

- Updated the author confirmation checklist from the obsolete v19 package reference to the immutable v22 archive workflow.
- Recorded the completed EKM-ED one-shot as a prospective structural endpoint failure and separated it from optional future experiments needed only for stronger external-effectiveness claims.
- Updated T8 traceability evidence and the anonymous repository validation commands to the v22 packager and validator.
- Made the EKM-ED validator ignore LaTeX-only `\allowbreak{}` markers when verifying the immutable failure-record hash, so visual line breaking and semantic hash validation remain consistent.
- Added the EKM-ED prospective endpoint-transport failure explicitly to the cover letter while preserving the non-effectiveness and non-safety claim boundary.

## 2026-09-16: prospectively locked EKM-ED structural endpoint test

- Downloaded the official 4,345,584,031-byte EKM-ED archive and verified MD5
  `521774df7dc91c31f2ba09fdefc299c6` and SHA-256
  `0b6b4cc003cf4b94b8af970590aecfd8f05dc98522c2b2ecd92bc523ae5471fa`.
- Recorded 1,954 ZIP central-directory members without opening participant
  files, then used the preauthorized header-only amendment to read exactly one
  SAM header and one deterministic 128-Hz clean Muse header. No data row was
  accessed before implementation locking.
- Corrected a pre-access branch-selector defect that would have confused
  `unclean-signals` with `clean-signals`; added regression coverage that rejects
  the unclean branch and the macOS metadata mirror.
- Implemented and synthetically tested the exact four-channel RAW adapter,
  public key-moment windows, SAM labels, archive inventory checks, task-specific
  eligibility, and progress reporting. Seven pre-lock tests passed on the RTX
  4070 Ti SUPER, producing implementation-lock SHA-256
  `ec729f31627033819af91368995089780c2825dac3cee3f0ae233d73bf380235`.
- Executed the first locked `prepare` stage without retuning. All 47
  questionnaire participants were attempted. Of 188 participant--film units,
  86 retained all three key-moment windows; 10 lacked a registered clean file,
  45 had zero complete finite windows, and 47 had one. After frozen midpoint
  exclusion, neither valence nor arousal had an eligible participant under the
  three-trial, both-class rule.
- Stopped before pre-outcome locking, emotion-model fitting, material-event
  construction, action selection, or effectiveness evaluation. Preserved the
  result as `failure_001_structural_ineligibility.json` with SHA-256
  `8f621780673a87e455594ec46e2bb38ef1538470f355471b46de0f64633cb070`.
- Revised the manuscript, supplement, evidence-role matrix, and T5 traceability
  to describe a prospective endpoint-transport failure. No effectiveness,
  non-harm, safety, or replicated external-validation claim was added.

## 2026-09-14: raw-partition-first independence closure

- Added a full EPPVR raw-partition-first sensitivity. The 30 participants were
  assigned outcome-blind to two disjoint 15-person populations, after which the
  complete identity-dose experiment, emotion models, identity probes, and risk
  tables were rebuilt independently in each population.
- Completed 9,600 emotion-model fits and 160 identity-probe fits. All MLP fits
  used the NVIDIA GeForce RTX 4070 Ti SUPER for 40 epochs, and a 9,760-row
  provenance ledger verified that no participant, raw row, training row, or
  fitted target-domain model crossed the population boundary.
- The audit-population CORAL and quantile OPCT mean shifts were -0.2122 and
  -0.2030, both outside the frozen absolute applicability limit of 0.10. The
  action therefore stopped before outcome-based release and returned the
  original probability for all 320 evaluation-population configurations.
- Added a four-level independence-ladder figure and table. The evidence now
  contracts from three transductive releases, to two covariate-unseen releases,
  to one raw-test-group release, and finally to no release under complete
  participant- and training-row separation.
- Independent validation passed 21/21 checks. The manuscript and supplement
  now state explicitly that this result demonstrates target-batch dependence
  and fail-closed protocol execution, not positive fully training-data-
  independent calibration, prospective external effectiveness, or safety.
- Replaced the remaining ``independently map'' wording with an explicit
  separately-constructed-witness boundary, moved the designated post-hoc 20\%
  reporting-cell detail out of the main Methods, and synchronized the
  Supplementary reproducibility section to the 252-file inventory (23/23) and
  consolidated reviewer-closure validation (14/14).
- Reduced main-text information load by removing the duplicated cross-fit
  stress-test and same-ceiling comparator figures. Their full figures and
  cell-level results remain in the Supplementary Material; no result, method,
  threshold, or conclusion changed.

## 2026-09-14: reviewer-closure sensitivity revision

- Added a configuration-level inductive target-transform sensitivity in which
  assignment is seed-only and held-out covariates and outcomes are excluded
  from transform fitting. EPPVR and CASE retained positive releases, CEAP
  returned the original probability, and both unsupported domains remained
  unchanged; independent validation passed 12/12 checks.
- Added a stricter raw-group independence sensitivity with disjoint test
  participants and, for CASE, CEAP, SEED-IV, and DREAMER, disjoint verified
  physical stimuli. Only EPPVR remained certified. Shared model-training rows
  and repeated split-seed fits are explicitly retained as unresolved limits;
  independent validation passed 13/13 checks.
- Added resource-matched CPCS-style and ATC-style nearest-neighbor analyses.
  All CPCS-style candidates failed the common release certificate and returned
  the original probability. ATC was retained as aggregate accuracy estimation,
  not a probability action. The TransCal input mismatch is now stated
  explicitly; independent validation passed 14/14 checks.
- Added crossed participant/stimulus bootstrap endpoint uncertainty and
  probabilistic-label action propagation. Almost all source intervals crossed
  the fixed 0.02 endpoint and no lower-confidence-bound event remained, while
  EPPVR and CASE retained positive soft-endpoint gains. The manuscript now
  treats the binary event as measurement-uncertain; the action-level validator
  passed 12/12 checks.
- Replaced the capability table's advantage-style entries with a neutral
  resource, decision-level, assumption, and empirical-role matrix. Redrew
  Figure 1 to expose transductive and inductive information-access boundaries,
  and narrowed the abstract, component-necessity, and released-action claims.
- Added a consolidated reviewer-closure validator that checks all four new
  analysis families, final LaTeX logs, figure freshness, bounded claims, and
  the remaining author-confirmation items.

## 2026-09-14: BSPC reviewer-boundary and presentation revision

- Retitled the manuscript to identify the transported object explicitly as an
  audit-risk probability rather than an EEG emotion prediction.
- Added the finite-sample estimand and its identification boundary for the
  matched identity-dose intervention. The text now excludes an unconditional
  causal or physiological interpretation.
- Specified the null hypotheses for paired bootstrap, sign-randomization, and
  exact McNemar inference. Replaced residual safety language with released-action
  risk language and added the observed 2.95--4.64% one-sided upper-bound range.
- Added Supplementary Table S8 documenting the provenance and inferential role
  of all endpoint, applicability, and release thresholds. The table states that
  freezing does not establish independent optimality and that post-hoc stability
  analyses did not select or retune thresholds.
- Compressed the AVDOS-VR, FACED, and EEGEmotions-27 main-text results into one
  applicability-boundary subsection. Detailed protocol histories, figures, and
  tables remain in the Supplementary Material.
- Rebuilt the main-text capability matrix without whole-table scaling and added
  a separate five-item BSPC Highlights file. Added nonprinting author TODOs for
  the funding statement and anonymized repository URL or archival DOI.
- Recompiled the 37-page manuscript and 36-page supplement with no LaTeX errors,
  undefined citations or references, overfull boxes, or oversized floats. Visual
  checks confirmed that Table 1 and Supplementary Table S8 are legible and not
  clipped. Scientific-readiness validation passed 12/12 checks and independent
  submission-artifact validation passed 78/78 checks.

## 2026-09-11: BSPC literature and evidence-integration revision

- Incorporated author-confirmed EPPVR metadata: DPVR E4 head-mounted display,
  ADS1299-based EEG acquisition front end, right-ear reference and ground, and
  approval by the Ethics Committee of the University of Electronic Science and
  Technology of China (approval no. 106142023122227999). Added a dedicated
  Ethics statement recording written informed consent and authorization for
  the secondary analysis. Channels 5--9 were identified collectively as two
  EOG, one PPG, one GSR, and one temperature channel; label column 3 was
  identified as dominance. Neither enters the present analysis. Removed the
  incomplete CRediT placeholder; contribution roles can be supplied through
  the Elsevier submission workflow after agreement among the authors.
- Promoted the validated 2026-09-10 v5 evidence-integration preview into the
  formal BSPC manuscript and supplementary sources, including the frozen
  EEGEmotions-27 boundary results and the latest figures and tables.
- Expanded the main-text literature base from 11 to 35 cited works. Added
  direct support for repeated-measures leakage and structured validation,
  shortcut learning, calibration under domain shift, target-performance
  estimation, selective prediction, conformal risk control, and the original
  CASE, CEAP-360VR, SEED-IV, DREAMER, AVDOS-VR, FACED, and EEGEmotions-27
  dataset papers.
- Added an explicit closest-family paragraph distinguishing DCS-OPCT from CPCS,
  TransCal, ATC, SelectiveNet, and conformal risk-control formulations. The
  novelty remains an end-to-end capability claim rather than component
  invention.
- Recompiled the 41-page formal manuscript under WSL TeX Live. The final log
  contains no LaTeX errors, undefined citations, undefined references,
  overfull boxes, or BibTeX warnings.
- Reworked the supplementary endpoint-harmonization longtable to fit the
  Elsevier text block, permit readable category wrapping, and repeat column
  headings on continuation pages. The 36-page supplement now compiles with no
  LaTeX errors, undefined references, or overfull boxes; pages 1--4 were also
  checked from rendered PNGs for clipping, overlap, and continuation layout.
- Preserved the scientific claim boundary and every frozen v11 result. EPPVR
  acquisition/ethics metadata and the final CRediT statement remain explicit
  author-confirmation requirements before submission.

## 2026-09-10: seven-domain retrospective robustness boundary

- Added a post-hoc seven-domain analysis spanning DEAP, MAHNOB-HCI, EPPVR,
  CASE, CEAP, SEED-IV, and DREAMER, with 1,920 subject-axis configurations.
- Retained the five frozen mechanism predictors and used nested
  leave-one-dataset-out threshold selection for pooled, equal-domain, and
  smooth worst-domain L2 logistic models. All 147 fits ran in deterministic
  float64 CUDA on the RTX 4070 Ti SUPER.
- Preserved the failed prespecified robustness gate: the equal-domain model
  reached median AUROC 0.616, median balanced accuracy 0.592, positive median
  Brier skill 0.0233, and AUROC at least 0.60 in four of seven domains. CASE
  remained the clearest transfer boundary.
- Recorded that broader pooling improved weighted Brier score by 0.0111 over
  the frozen two-source probabilities but did not establish transferable
  ranking or operating points. The result is boundary evidence, not a rescue
  analysis or prospective external confirmation.
- Added a four-panel supplementary figure and a domain-level table. Independent
  validation passed 17/17 analysis checks and 11/11 artifact checks. Three
  execution defects and the first visually overlapping figure remain preserved
  as failure records.
- Added the result and explicit failed-gate language to the manuscript and
  supplement without modifying the frozen v11 manifest, thresholds, actions,
  or held-out decisions. The compiled manuscript is 31 pages and the
  supplement is 19 pages.
- Prepared a new immutable `dcs_opct_v11_submission_artifacts_crossfit_v12`
  package that extends, rather than replaces, the validated v11 package and
  retains the complete v12 protocol, row-level predictions, coefficients,
  figures, tables, validators, and failure history.

## 2026-09-09: frozen leave-one-component diagnostic

- Added a CUDA reconstruction and diagnostic script plus an independent
  validator; the reconstruction had to reproduce the archived component
  diagnostics and action locks before any counterfactual analysis.
- Removed the quantile witness without changing the CORAL candidate, thresholds,
  v11 assignment, certificate, or held-out rows. DREAMER became audit-eligible
  but was rejected at audit gain -0.00047 and lower bound -0.00413.
- Removed the complete unlabeled applicability screen under the same frozen
  design. SEED-IV became audit-eligible but was rejected at audit gain -0.03146
  and lower bound -0.04458.
- Replaced only the distribution-covered assignment with 100 archived random
  balanced half-audits. CEAP release fell to 0.60, whereas the deterministic
  distribution-covered assignment certified a held-out gain of 0.00846.
- No released action in these diagnostics had material negative transfer. This
  is retrospective component-role evidence, not causal necessity or prospective
  safety evidence. Independent validation passed 17/17 checks.
- Added two supplementary tables and bounded interpretation to the Results and
  Discussion.
- Issued `dcs_opct_v11_submission_artifacts_crossfit_v10` with the diagnostic
  CSVs, tables, manifest, and validation report. Independent submission-package
  validation expanded to 78/78 checks; `crossfit_v9` remains preserved.
- Issued immutable `dcs_opct_v11_submission_artifacts_crossfit_v11` after the
  independent component validator refreshed its generated timestamp. The
  scientific artifacts and frozen decisions are unchanged; `crossfit_v10`
  remains preserved.
- Made the leave-one-component validator idempotent: rerunning unchanged 17/17
  checks preserves the report timestamp and byte-level hash instead of
  invalidating an immutable submission package.
- Re-ran the CUDA release-readiness audit at 5/5 gates, validated the v11
  submission package at 78/78 checks, and rebuilt the 41-file reproducibility
  inventory at 16/16 checks. The frozen v11 SHA-256 remained unchanged.

## 2026-09-09: independent reproducibility inventory

- Added `scripts/build_dcs_opct_v11_reproducibility_inventory.py` and
  `scripts/validate_dcs_opct_v11_reproducibility_inventory.py`.
- Built a non-self-referential evidence inventory covering 41 unique files:
  the readiness driver and report, dataset-availability matrix, the 13-group
  AMIGOS and 15-group Emognition pre-access stack reports, prospective one-shot
  source and tests, every artifact
  named by the frozen v11 manifest, the validated submission manifest/report,
  and the manuscript and supplementary TEX/PDF files.
- Excluded the inventory and its validation report from membership and kept the
  release-readiness audit independent of the inventory, preventing circular
  hashes.
- Independently checked required path membership, evidence grouping, byte counts,
  SHA-256 digests, the complete frozen-artifact map, both pre-access stacks,
  submission validation, and compiled PDF signatures.
- Clarified the abstract as two public-source leave-dataset-out development
  tests and linked Supplementary Figure 6 to the corresponding numerical and
  paired-inference table.
- Recompiled the 30-page manuscript and 16-page supplement before inventory
  generation. The frozen v11 SHA-256 remained
  `fe0d6b2f3bb7a90da51fd7b030f0f4874d2dee9ab14175a20f9f4e8d0d71b0ee`.

## 2026-09-09: prospective wearable pipeline integrity hardening

- Repaired the formal `lock_unlabeled_action_from_bundle` entry point: its
  action-fitting block had been unreachable after a neighboring function's
  return despite lower-level tests passing.
- Added a CUDA regression path that invokes the formal entry point with the
  frozen public risk table, frozen model, manifest, and unchanged v11 freeze.
- Extended action-lock verification from the lock JSON alone to all four
  locked action artifacts. Training and finalization now reject any checksum
  change in mechanism probabilities, component diagnostics, cluster geometry,
  or the distribution-covered assignment.
- Added dataset-neutral adapter-output validation plus separate AMIGOS and
  Emognition contracts. Synthetic failure injection rejects duplicate trials,
  label drift, unremoved midpoint labels, single-class participants, missing
  representations, and nonfinite features before dose construction.
- Closed each pre-access report's executed-code provenance by separating the
  dataset tests and hashing the stack validator itself.
- Added a schema-bound implementation-lock generator and separate AMIGOS and
  Emognition lock entry points. The lock binds the schema manifest, reservation,
  frozen v11 file, analysis and dependency code, executed-test hashes, CUDA
  runtime, and explicit no-participant-value-access attestation.
- Added failure injection for participant-value fields hidden inside nested
  schema metadata, missing attestation, output overwrite, post-lock code
  changes, and a test script that modifies itself while executing.
- Extended the implementation lock from the freeze file itself to every artifact
  listed in the frozen inventory. The one-shot path now re-verifies that complete
  inventory before preparation, blinded action selection, model-outcome fitting,
  and final endpoint evaluation.
- Added adapter-to-pre-outcome audit propagation: each task-specific adapter
  audit is embedded in the pre-outcome bundle and protected by the bundle hash
  before any material-event outcome is constructed.
- Added a full synthetic CUDA one-shot regression using all five frozen seeds,
  the frozen risk model and public risk table, 1,000 OPCT epochs, 240 actual
  logistic fits, material-event construction, the final endpoint gate, and the
  identity-fallback boundary. The first deliberately incomplete one-seed fixture
  was rejected by the frozen distribution-coverage audit; the corrected test
  restores the five registered seeds without weakening that audit.
- Refreshed the prospective external-family stacks to 13/13 for AMIGOS and
  15/15 for Emognition on the RTX 4070 Ti SUPER with PyTorch 2.6.0+cu124. Each
  report hashes the implementation-
  lock code, one-shot core, their tests, the full CUDA end-to-end test, and the
  stack validator itself. These are software-readiness results only; neither
  archive is locally available and no external participant value or effectiveness
  outcome was accessed.
- Added `scripts/validate_dcs_opct_v11_release_readiness.py` as a single-command
  pre-submission audit. Its default path compiles the scripts, runs Ruff on the
  safety-critical stack, refreshes the 13-group AMIGOS and 15-group Emognition
  CUDA pre-access suites, verifies
  their current evidence hashes, rechecks the frozen manifest, and reruns the
  submission-artifact validator with a `tqdm` progress bar. The complete run
  passed 5/5 gates in 79.473 seconds.
- Added `docs/dcs_opct_v11_dataset_availability_matrix.md` to distinguish raw-
  data access, releasable derivatives, evidence role, and inferential boundary
  for all completed and prospectively reserved datasets. The manuscript,
  supplement, and README now point to the one-command audit and this matrix.
- Recompiled the updated 30-page manuscript and 15-page supplement under WSL
  TeX Live. Final logs contain no LaTeX errors, undefined citations or references,
  or overfull boxes.
- Re-ran independent submission validation at 68/68 checks and confirmed the
  frozen v11 SHA-256 remains
  `fe0d6b2f3bb7a90da51fd7b030f0f4874d2dee9ab14175a20f9f4e8d0d71b0ee`.

## 2026-09-09: FACED post-access common-montage boundary

- Preserved the registered FACED 32-channel structural eligibility failure:
  61 participants used auricular A1/A2 channels, whereas 62 used horizontal EOG
  channels, leaving only 30 physiologically comparable EEG channels.
- Added the post-access exploratory 30-channel analysis of 123 participants,
  2,952 participant--video rows, and 4,500 GPU fits.
- Recorded identity-probe balanced accuracy of 0.885--0.992 against chance
  0.008, but zero material events across 120 nonzero-dose configurations.
- Recorded frozen CORAL and quantile probability shifts of 0.122 and 0.334,
  both above the 0.10 applicability budget; the selected action remained
  identity and calibration effectiveness was non-estimable.
- Added the FACED boundary figure and dose table to the manuscript and
  supplement without changing the frozen v11 manifest or any release threshold.
- Expanded the submission package to include FACED evidence, failure records,
  locks, source hashes, figure, tables, and validation report. Independent
  submission-artifact validation passed 68/68 checks.
- Issued the traceable `crossfit_v9` package after replacing the action
  matrix's ambiguous ``Safe=PASS'' display for identity fallback with
  released-action non-harm shown as N/A whenever no correction was released.
  The earlier `crossfit_v8` package remains preserved.
- Recompiled the final 29-page manuscript and 15-page supplement. Final logs
  contain no LaTeX errors, undefined citations or references, or overfull
  boxes.
- Replaced residual manuscript wording that could conflate abstention with
  safety by using `conditional non-intervention`, `identity action`, and
  `bounded non-harm evidence`; no numerical result or frozen decision changed.

## 2026-09-08: material-event threshold robustness

- Added `scripts/analyze_material_event_threshold_robustness.py`.
- Added a post-hoc engineering anchor linking the frozen 0.02 endpoint to candidate-model margins, risk-ranked audit allocation, and bounded decision utility; independent validation passed 12/12 checks.
- Repeated leave-dataset-out validation at material-event thresholds 0.010,
  0.015, 0.020, 0.025, and 0.030 with 5,000 cluster-bootstrap repetitions.
- Preserved the frozen DCS-OPCT v11 model, 0.020 endpoint, operating threshold,
  gates, external decisions, and predecessor failures unchanged.
- Found that 0.020 and 0.025 met all descriptive metric floors and retained at
  least 20 events per held-out dataset. Lower thresholds failed metric floors;
  0.030 left only eight DEAP events.
- Added a readable two-panel supplementary table and a four-panel robustness
  figure. The analysis is labeled post hoc and does not claim physiological
  materiality, threshold optimality, universal safety, or expanded external
  effectiveness.
- Recompiled the 24-page manuscript and 10-page supplement. Final logs contain
  no LaTeX errors, undefined citations or references, or overfull boxes.
- Independent submission-artifact validation remains 23/23 passed.

## 2026-09-08: stronger matched-budget calibration baselines

- Added `scripts/analyze_dcs_opct_v11_strong_calibration_baselines.py` without
  modifying any frozen v11 artifact or decision.
- Added GPU monotone beta calibration, split-fit/certified monotone beta
  calibration, and positive temperature scaling to the existing matched-budget
  Platt, isotonic, and DCS comparisons.
- Retained the same frozen audit and held-out halves, 20--100% nested budgets,
  100 audit-order repetitions, 2,000 two-stage bootstrap repetitions, Brier
  certificate threshold of 0.001, material-negative threshold of -0.001, and
  AUROC noninferiority margin of -0.02.
- Reproduced all 125 legacy summary cells across 11 numeric fields with maximum
  absolute discrepancy `9.95e-17`.
- At the 20% budget, direct beta calibration produced material negative transfer
  in 6%, 69%, 53%, 6%, and 28% of repetitions for EPPVR, SEED-IV, DREAMER,
  CASE, and CEAP. Selective DCS-OPCT remained at zero in all five domains.
- Over the equal-domain 20% and 40% cells, direct Platt and beta achieved mean
  Brier gains of 0.0181 and 0.0174 but material-negative frequencies of 0.263
  and 0.268. Selective DCS-OPCT achieved 0.0134 with zero material-negative
  events.
- The manuscript now states explicitly that direct Platt and beta are more
  effective in four domains at full label budget. The supported contribution is
  selective low-label risk control, not universal calibration superiority.
- Replaced the supplementary comparator plots with strong-baseline direct and
  split-certified figures and added a low-label summary table.
- Added the strong-baseline figures, result tables, manifest, and legacy
  reproduction record to the submission artifact package. The independent
  validator now checks the frozen-manifest hash, minimum-budget safety result,
  beta frequencies, copied-file hashes, generated table content, and every
  manifest-listed output hash (36 of 36 checks passed).
- Added the exact CUDA reproduction and package-validation commands to the
  repository README.

## 2026-09-08: same-audit cross-fit certification stress test

- Added GPU two-fold cross-fit-certified Platt and monotone beta comparators.
- Used opposite-fold predictions for certification over the complete audit label budget, then
  refitted a passing map on all audit labels for held-out release.
- Added 20,000-repetition paired bootstrap intervals, 100,000 sign-randomization tests, exact
  McNemar safety tests, and Holm correction across 150 comparison cells.
- Preserved the result that cross-fit certification still released harmful corrections in
  SEED-IV, DREAMER, and CEAP at the 20% budget.
- Added one three-panel main-text stress-test figure and a supplementary paired-inference table.
- Added one-sided released-action safety upper bounds; zero observed DCS events now carries an
  explicit 2.95%--4.64% descriptive upper-bound range.
- Expanded independent artifact validation from 36 to 46 checks; all checks passed.
- Recompiled the 26-page manuscript and 11-page supplement with no LaTeX errors, undefined
  citations or references, or overfull boxes.
